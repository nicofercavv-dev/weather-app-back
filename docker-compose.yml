version: '3.8'
services:
  db:
    image: postgres:18
    container_name: meteorologia-postgres
    environment:
      POSTGRES_DB: meteorologia_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"